name: flutter_bluetooth_chat
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:

  flutter_map: ^1.0.0

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  all_bluetooth: ^0.1.0
  permission_handler: ^11.2.0
  flutter_chat_bubble: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^3.0.1

  flutter_launcher_icons: "^0.9.2"

flutter_icons:
  android: true
  ios: true
  image_path: "lib/image/logomyseacom_square.png"
flutter:
  uses-material-design: true
  assets:
    - lib/image/